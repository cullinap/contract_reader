from setuptools import setup

setup(
    name="contract_reader",
    version="1.0",
    description="A useful module",
    author="Patrick Cullinane",
    author_email="",
    packages=["contract_reader"],  # same as name
)
